{
  "name": "guarantee-docs",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vocs dev",
    "build": "vocs build",
    "preview": "vocs preview",
    "deploy": "vocs build && npx wrangler pages deploy docs/dist --project-name=guarantee-docs"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "vocs": "^1.4.1"
  }
}
